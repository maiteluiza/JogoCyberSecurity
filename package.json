{
  "name": "JogoCyberSecurity",
  "private": true,
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "predeploy": "cd Quiz && npm install && npm run build",
    "deploy": "gh-pages -d Quiz/dist"
  }
}
